{
  "name": "html-to-img",
  "version": "1.0.0",
  "description": "Example Express + Puppeteer server with a simple client to capture HTML blocks as images.",
  "main": "server.js",
  "scripts": {
    "start:backend": "node server.js",
    "start:frontend": "npx serve public",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.13.1"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
